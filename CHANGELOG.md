# Changelog

## Unreleased

- P0 scaffold: pinned requirements, `pyproject.toml`, `.env.example`, configuration
  loader with placeholder detection, profiles, `cli init-env` and `cli check`,
  language and secret checks, CI workflow, Thunder Client collection.
