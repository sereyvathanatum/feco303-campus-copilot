# Build-path step 2: Extract text from PDF and Markdown.
# Capabilities built so far; see docs/build-path/.
step = 2
title = "Extract text from PDF and Markdown"
capabilities = ["kb"]

[ingest]
until = "extract"
