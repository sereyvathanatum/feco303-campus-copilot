# Build-path step 6: Remember the conversation.
# Capabilities built so far; see docs/build-path/.
step = 6
title = "Remember the conversation"
capabilities = ["kb", "rag", "memory"]

[rag]
condense_query = "always"
